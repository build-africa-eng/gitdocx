# escape=`
FROM africanfuture/windows-devbox-base 

# Use Windows native shell
SHELL ["cmd", "/S", "/C"]

# Use ContainerAdministrator to avoid ContainerUser issue in CI
USER ContainerAdministrator

# Simple output to confirm build
RUN echo Hello from GitDocx Windows > C:\hello.txt

# Default command
CMD ["cmd.exe"]