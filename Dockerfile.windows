# escape=`
FROM africanfuture/windows-devbox-base:latest

SHELL ["cmd", "/S", "/C"]

# (no USER line here—use whatever the base image already has)

# Create the file
RUN echo Hello from GitDocx Windows > C:\hello.txt

CMD ["cmd.exe"]