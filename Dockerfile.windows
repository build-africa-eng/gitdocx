# escape=`
FROM africanfuture/windows-devbox-base 

# Set default shell
SHELL ["cmd", "/S", "/C"]

# Set a valid Windows container user
USER ContainerUser

# Create a file
RUN echo Hello from GitDocx Windows > C:\hello.txt

CMD ["cmd.exe"]
