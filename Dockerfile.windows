# escape=`
FROM mcr.microsoft.com/windows/nanoserver:ltsc2022

SHELL ["cmd", "/S", "/C"]

# Explicitly run under Administrator
USER Administrator

RUN echo Hello from GitDocx Windows > C:\hello.txt

CMD ["cmd.exe"]
